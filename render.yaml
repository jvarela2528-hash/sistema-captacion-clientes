services:
  - type: web
    name: sistema-captacion-clientes
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node whatsapp-bridge.js
    envVars:
      - key: NODE_ENV
        value: production
